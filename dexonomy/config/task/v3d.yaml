max_num: 100
data_type: succ_grasp # Choices: [init, grasp, succ_grasp, succ_traj, init_template, new_template]
check_success:    # Choices: [True, False, None]

hand:
  mode: visual  # Choices: [visual, collision]
  init_body: False
  contact: False
  skeleton: False

object:
  contact: False

cust_log_dir: ${log_dir}
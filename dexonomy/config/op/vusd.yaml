max_num: 100
data_type: succ_grasp # Choices: [init, grasp, succ_grasp, succ_traj, new_template]
check_success:    # Choices: [True, False, None]
qpos_type: ["grasp"] # Choices: combination from [grasp, pregrasp, squeeze, traj]

hand:
  mode: visual  # Choices: [visual, collision]

cust_log_dir: ${log_dir}
